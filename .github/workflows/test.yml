name: test
on: [push, pull_request, workflow_dispatch]

jobs:
  pytest:
    name: pytest
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        python-version: [3.11]
        
    steps:
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      - name: Checkout 🛎️
        uses: actions/checkout@v2
        
      - name: Install Dependencies
        run: |
          pip install -U pytest pytest-cov
          pip install -U -r requirements.txt
          pip install -e .  # ← ВАЖНО: ставим сам пакет

      - name: Testing
        run: |
          pytest test/ --cov=mylib --cov-report=xml  # без PYTHONPATH
